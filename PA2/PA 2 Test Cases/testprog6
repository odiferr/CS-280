PROGRAM circle;
	(*Missing operand after operator*)
VAR
	(*Variable declarations*)
	r, a, p, b : REAL; 
	i, j, sum : INTEGER;	
BEGIN
	r := 8;
	p := 0;
	a := 0;
	sum := 0;
	j := -1;
	IF ( r > 5) THEN
	  	a := (3.14) +  * r
	ELSE
		p := 0;
		
	(*Display the results*)
	WRITELN  ('The output results are ' , r, p, b);
END
