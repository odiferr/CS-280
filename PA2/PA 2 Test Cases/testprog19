PROGRAM circle;
	(*Clean Program*)
const
	pi = 3.14;
	factor = 2;
VAR
	(*Variable declarations*)
	r, a, p, b : REAL; 
	i, j, sum : INTEGER;
	str1, str2: STRING;	
BEGIN
	r := 8;
	p := 0;
	a := 0;
	sum := 0;
	str1 := 'Hello World!';
	j := -1;
	IF ( r > 5) THEN
		begin
	  	a := pi + j *j - r;
	  	write('Result: ', a)
	  	end
	ELSE
		IF (r = 0 ) THEN p := 0;	
	
	(*Display the results*)
	WRITELN  ('The output results are ' , r, str1, b);
END.
            